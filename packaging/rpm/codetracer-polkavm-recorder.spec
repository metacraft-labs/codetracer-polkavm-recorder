Name:           codetracer-polkavm-recorder
Version:        0.1.0
Release:        1%{?dist}
Summary:        CodeTracer recorder for PolkaVM programs (RISC-V based virtual machine)

License:        Apache-2.0
URL:            https://github.com/metacraft-labs/codetracer-polkavm-recorder

# Pre-built binary path is injected at rpmbuild-time by
# packaging/rpm/build.sh through `--define "_binary_source <path>"`.
# This avoids carrying a Source0 tarball through the spec for a
# binary-only recorder package.

# Recorder runtime dependencies, materialised by generate.py from
# the per-recorder metadata's dependencies.rpm list.
Requires:       glibc
Requires:       libzstd

%description
CodeTracer recorder for PolkaVM programs (RISC-V based virtual machine)

This package is part of the CodeTracer record/replay debugging
suite. Install the launcher (codetracer) alongside this package for
a full record/replay workflow.

%install
mkdir -p %{buildroot}/usr/bin
install -m 0755 %{_binary_source} %{buildroot}/usr/bin/codetracer-polkavm-recorder

%files
/usr/bin/codetracer-polkavm-recorder

%changelog
* Thu Jun 04 2026 Metacraft Labs <ci@metacraft-labs.com> - 0.1.0-1
- Generated by codetracer-launcher recorder-templates (M11).
